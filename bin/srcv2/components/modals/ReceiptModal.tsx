
interface ReceiptModalProps{
    tableID: string;
    handleLoadProp: (loadState: boolean) => void;
    handleExit: (loadState: boolean) => void;
};

export const ReceiptModal = ({tableID, handleLoadProp, handleExit}: ReceiptModalProps) => {
    return (
        <>
            
        </>
    );
};